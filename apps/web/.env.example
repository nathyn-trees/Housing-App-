DATABASE_URL="postgresql://nearby:nearby@localhost:5432/nearby"
JWT_SECRET="change-me-to-a-long-random-string"
APP_URL="http://localhost:3000"
ADMIN_EMAIL="you@example.com"

# Optional: without RESEND_API_KEY, "sent" emails just log to the server
# console (see apps/web/lib/mailer.ts) — fine for local dev, not for real
# users. EMAIL_FROM needs a domain verified in Resend once you're past their
# sandbox sender.
RESEND_API_KEY=""
EMAIL_FROM="Nearby <onboarding@resend.dev>"
